# Crypto Token Analyzer

This project analyzes new tokens from the `pump.fun` website, checks their contract security using the RugCheck API, verifies Twitter audience quality via the TweetScout API, and sends valid tokens to a Telegram bot.

## Requirements
- Python 3.x
- Install dependencies: